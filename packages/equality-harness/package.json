{
  "name": "@compiled/equality-harness",
  "version": "0.0.1",
  "private": true,
  "description": "Byte-equality harness: runs every fixture in /fixtures through Babel with [@atlaskit/tokens, @compiled/babel-plugin] under both COMPILED_CSS_ENGINE off and on, asserting identical bytes.",
  "license": "Apache-2.0",
  "scripts": {
    "verify": "bun run scripts/verify.mjs",
    "verify:bail": "bun run scripts/verify.mjs --bail"
  },
  "dependencies": {
    "@atlaskit/tokens": "*",
    "@babel/core": "^7.26.10",
    "@compiled/react": "workspace:*",
    "@compiled/babel-plugin": "workspace:*",
    "@compiled/css-native": "workspace:*"
  }
}
