// Resolver-matrix axis-6 fixture consumer. Verifies that with
// extensions=['.js','.jsx','.ts','.tsx'] in priority order, foo.js
// wins over foo.ts when both exist on disk.
require('./shared/util');
