//! Port of `packages/css/src/plugins/flatten-multiple-selectors.ts`.

use postcss_core::Root;

pub fn flatten_multiple_selectors(_root: &mut Root) {
    unimplemented!("Phase 4b — port flatten-multiple-selectors.ts");
}
