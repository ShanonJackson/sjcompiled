//! Byte-for-byte Rust port of `postcss-minify-params@5.1.4`.
//! Phase 6f — full port pending. Browserslist-aware (caniuse-api gates
//! several decisions). Minifies @media / @supports params.

use postcss_core::{PluginResult, Root};

pub fn postcss_minify_params(_root: &mut Root) -> PluginResult {
    unimplemented!("Phase 6f — port postcss-minify-params@5.1.4")
}
