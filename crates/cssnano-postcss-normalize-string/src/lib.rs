//! Byte-for-byte Rust port of `postcss-normalize-string@5.1.0`.
//! Phase 6b — full port pending. Walks decl values via
//! postcss-value-parser, normalizing string-literal quote style.

use postcss_core::{PluginResult, Root};

pub fn postcss_normalize_string(_root: &mut Root) -> PluginResult {
    unimplemented!("Phase 6b — port postcss-normalize-string@5.1.0")
}
