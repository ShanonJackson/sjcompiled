[package]
name = "babel-plugin-phase0-probes"
description = "Phase 0 sandbox/WASI/postcard probes (PLAN.md §3.9.14). Not shipped."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
swc_core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
postcard = { workspace = true }
indexmap = { workspace = true }

# Build:    RUSTFLAGS="" cargo build -p babel-plugin-phase0-probes --target wasm32-wasip1 --release
# Output:   crates/target/wasm32-wasip1/release/babel_plugin_phase0_probes.wasm
