//! Byte-for-byte Rust port of `postcss-normalize-unicode@5.1.1`.
//! Phase 6e — full port pending. Browserslist-aware — gates output
//! based on browser support for unicode-range syntax.

use postcss_core::{PluginResult, Root};

pub fn postcss_normalize_unicode(_root: &mut Root) -> PluginResult {
    unimplemented!("Phase 6e — port postcss-normalize-unicode@5.1.1")
}
