//! Byte-for-byte Rust port of `postcss-normalize-positions@5.1.1`.
//! Phase 6b — full port pending. Normalizes `background-position` /
//! similar position keywords (left top → 0% 0%, etc.).

use postcss_core::{PluginResult, Root};

pub fn postcss_normalize_positions(_root: &mut Root) -> PluginResult {
    unimplemented!("Phase 6b — port postcss-normalize-positions@5.1.1")
}
