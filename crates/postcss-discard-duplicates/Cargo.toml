[package]
name = "postcss-discard-duplicates"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Byte-for-byte Rust port of postcss-discard-duplicates@6.0.0 (used by sort.ts; v6, NOT v5)."

[dependencies]
postcss-core = { workspace = true }
