import { cssMap } from '@compiled/react';

const styles = cssMap({
  primary: {
    color: 'blue',
    backgroundColor: 'white',
    padding: '8px 16px',
    borderRadius: '4px',
  },
  secondary: {
    color: 'gray',
    backgroundColor: 'lightgray',
    padding: '8px 16px',
    borderRadius: '4px',
  },
});
