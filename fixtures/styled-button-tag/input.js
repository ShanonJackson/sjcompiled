import { styled } from '@compiled/react';

const StyledButton = styled.button({
  backgroundColor: 'blue',
  color: 'white',
  padding: '8px 16px',
  borderRadius: '4px',
  border: 'none',
  cursor: 'pointer',
});
