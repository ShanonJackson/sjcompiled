import * as React from 'react';
import { css } from '@compiled/react';

const Header = () => (
  <header css={{
    backgroundColor: '#333',
    color: 'white',
    padding: '20px',
    textAlign: 'center',
  }}>
    My Website
  </header>
);
