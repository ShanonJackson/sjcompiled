import { styled } from '@compiled/react';

const Badge = styled.span({
  display: 'inline-block',
  padding: '2px 8px',
  borderRadius: '12px',
  backgroundColor: 'green',
  color: 'white',
  fontSize: '12px',
});
