import { styled } from '@compiled/react';

const bgColor = 'white';
const textColor = 'black';

const Card = styled.div({
  backgroundColor: bgColor,
  color: textColor,
  padding: '16px',
  borderRadius: '8px',
});
