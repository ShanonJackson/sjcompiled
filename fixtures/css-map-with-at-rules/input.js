import { cssMap } from '@compiled/react';

const responsiveStyles = cssMap({
  mobile: {
    fontSize: '14px',
    padding: '8px',
  },
  desktop: {
    fontSize: '18px',
    padding: '16px',
  },
});
